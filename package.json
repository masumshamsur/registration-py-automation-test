{
  "name": "regi-py-automation",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui-regression.spec.ts",
    "test:api": "playwright test tests/api-regression.spec.ts",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "typescript": "^5.6.3"
  }
}
